% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maps.R
\name{ShapeMap}
\alias{ShapeMap}
\title{Plot Shapefile with Map Elements}
\usage{
ShapeMap(
  shapefile_path,
  xlim = NULL,
  ylim = NULL,
  add_north = TRUE,
  add_scale = TRUE,
  north_pos = "tl",
  scale_pos = "bl",
  fill_color = "grey90",
  border_color = "black",
  xlab = "Longitude",
  ylab = "Latitude"
)
}
\arguments{
\item{shapefile_path}{Character. Full path to the shapefile (.shp file).}

\item{xlim}{Numeric vector of length 2. Longitude limits c(min, max).
If NULL, uses full extent (default = NULL).}

\item{ylim}{Numeric vector of length 2. Latitude limits c(min, max).
If NULL, uses full extent (default = NULL).}

\item{add_north}{Logical. Add north arrow? (default = TRUE).}

\item{add_scale}{Logical. Add scale bar? (default = TRUE).}

\item{north_pos}{Character. North arrow position: "tl" (top-left), "tr" (top-right),
"bl" (bottom-left), "br" (bottom-right) (default = "tl").}

\item{scale_pos}{Character. Scale bar position: same options as north_pos (default = "bl").}

\item{fill_color}{Character. Fill color for polygons (default = "grey90").}

\item{border_color}{Character. Border color for polygons (default = "black").}

\item{xlab}{Character. X-axis label (default = "Longitude").}

\item{ylab}{Character. Y-axis label (default = "Latitude").}
}
\value{
A ggplot object.
}
\description{
Creates a map from a shapefile with optional coordinate limits, north arrow,
and scale bar. This is a wrapper around sf and ggplot2 for quick shapefile
visualization.
}
\details{
This function reads a shapefile and creates a publication-ready map with
optional north arrow and scale bar. Requires the sf and ggspatial packages.

The function does NOT install packages automatically. Make sure you have
installed sf and ggspatial before using:
\code{install.packages(c("sf", "ggspatial"))}
}
\examples{
\dontrun{
# Example 1: Use included Iceland shapefile
iceland_path <- get_example_shapefile()
ShapeMap(iceland_path)

# Example 2: Zoom to specific region
ShapeMap(iceland_path,
         xlim = c(-27.9, -12.5),
         ylim = c(63, 67))

# Example 3: Custom colors
ShapeMap(iceland_path,
         xlim = c(-27.9, -12.5),
         ylim = c(63, 67),
         fill_color = "lightblue",
         border_color = "navy",
         north_pos = "tr")

# Example 4: Your own shapefile
ShapeMap("path/to/your/shapefile.shp")
}

}
\seealso{
\code{\link[sf]{st_read}} for reading shapefiles,
\code{\link[ggplot2]{geom_sf}} for plotting spatial data
}
